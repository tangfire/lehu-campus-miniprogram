<view class="page mine">
  <view class="profile panel">
    <image wx:if="{{user && user.avatar}}" class="avatar" src="{{user.avatar}}" mode="aspectFill" />
    <image wx:else class="avatar logo-avatar" src="/assets/logo-192.png" mode="aspectFit" />
    <view class="profile-main">
      <view class="name">{{user ? (user.nickname || user.name || '深汕同学') : '深汕 e站'}}</view>
      <view class="muted">{{profile ? profile.school_name : '登录后同步课表、收藏攻略、参与同学圈'}}</view>
    </view>
  </view>

  <button wx:if="{{!token}}" class="primary-btn" loading="{{loggingIn}}" bindtap="login">微信登录</button>
  <button wx:else class="ghost-btn" bindtap="logout">退出登录</button>

  <view class="panel menu" wx:if="{{token}}">
    <view class="menu-row" bindtap="editProfile">
      <text>编辑校园资料</text>
      <text>›</text>
    </view>
    <view class="menu-row" bindtap="goFreshmanKit">
      <text>新生工具包</text>
      <text>›</text>
    </view>
    <view class="menu-row" bindtap="goMyPosts">
      <text>我的帖子</text>
      <text>›</text>
    </view>
    <view class="menu-row" bindtap="goMyCollections">
      <text>我的收藏</text>
      <text>›</text>
    </view>
    <view class="menu-row" bindtap="goModeration">
      <text>内容审核</text>
      <text>›</text>
    </view>
  </view>

  <view class="panel info" wx:if="{{profile}}">
    <view class="row">
      <text>认证状态</text>
      <text>{{profile.auth_status === 1 ? '已认证' : '待校园认证'}}</text>
    </view>
    <view class="row">
      <text>宿舍</text>
      <text>{{profile.dorm_building || '未填写'}} {{profile.room_no || ''}}</text>
    </view>
    <view class="row">
      <text>班级</text>
      <text>{{profile.class_name || '未填写'}}</text>
    </view>
  </view>
</view>
